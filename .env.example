# Optional CoinGecko Demo API key — raises rate limits when present.
# Server-only: never prefix with VITE_/PUBLIC_, never import into client code.
COINGECKO_API_KEY=

COINGECKO_BASE_URL=https://api.coingecko.com/api/v3
